{
  "name": "homie-firestore-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Firestore security-rules tests (run against the local emulator).",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-homie \"vitest run\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "vitest": "^2.1.0"
  }
}
